/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/doingmythang',
    assetPrefix: '/doingmythang/',
    trailingSlash: true
};

export default nextConfig;
